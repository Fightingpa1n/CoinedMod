package net.fightingpainter.mc.coined.util;


/**
 * The MouseButton enum is used to determine which mouse button was clicked
*/
public enum MouseButton { //enum to represent the mouse buttons more clearly
    LEFT_CLICK, //left click is like putting down the whole stack you are carrying, or picking up a whole stack from a slot
    RIGHT_CLICK //right click is like putting down one item from the stack you are carrying, or picking up half of the items in a slot
}


//I'm sorry for this class being so short, and being kinda in the middle of stuff, and all around being abnoxious but I have to define it here for now as inside the mixin will cause errors
//maybe I can move it at somepoint to somewhere it makes more sense but for now it's here...